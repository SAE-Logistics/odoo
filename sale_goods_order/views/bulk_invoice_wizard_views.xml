<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sale_bulk_invoice_wizard_form" model="ir.ui.view">
        <field name="name">sale.bulk.invoice.wizard.form</field>
        <field name="model">sale.bulk.invoice.wizard</field>
        <field name="arch" type="xml">
            <form string="Bulk Invoice Orders">
                <group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="invoice_date"/>
                    </group>
                    <group>
                        <field name="order_family"/>
                        <field name="partner_id"/>
                        <field name="only_confirmed"/>
                    </group>
                </group>
                <footer>
                    <button name="action_generate_invoices" type="object" string="Generate Invoices" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_sale_bulk_invoice_wizard" model="ir.actions.act_window">
        <field name="name">Bulk Invoice Orders</field>
        <field name="res_model">sale.bulk.invoice.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
